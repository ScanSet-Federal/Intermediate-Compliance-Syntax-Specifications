DEF
CRI OR
CTN appcmdlistconfig
    TEST `at least one exists` `none satisfy`
    STATE ste_100
        result string equals `\[mimeMap\]\s+fileExtension:"\.(exe|dll|com|bat|csh)"\s+mimeType:"[^"]*"`
    STATE_END
    OBJECT obj_100
        identifier_type `Site`
        identifier `.*`
        section `system.webServer/staticContent`
        parameter `all`
    OBJECT_END
CTN_END
CRI AND
CTN service
    TEST `at least one exists` all
    OBJECT obj_31
        service_name `WsusService`
    OBJECT_END
CTN_END
CTN appcmdlistconfig
    TEST `at least one exists` `none satisfy`
    STATE ste_101
        result string equals `\[mimeMap\]\s+fileExtension:"\.(dll|com|bat|csh)"\s+mimeType:"[^"]*"`
    STATE_END
    OBJECT obj_100
        identifier_type `Site`
        identifier `.*`
        section `system.webServer/staticContent`
        parameter `all`
    OBJECT_END
CTN_END
CRI_END
CRI_END
DEF_END