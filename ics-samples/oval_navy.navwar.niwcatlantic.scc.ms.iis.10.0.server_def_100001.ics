DEF
CRI AND
CTN cmdlet
    TEST `at least one exists` all
    STATE ste_242
        value record equals `
    ` `all`
    STATE_END
    OBJECT obj_242
        module_name `ServerManager`
        module_id ``
        module_version ``
        verb `Get`
        noun `windowsfeature`
        parameters record
            field `Web-Net-Ext45,Web-Net-Ext`
        select record
            field `installstate`
    OBJECT_END
CTN_END
CRI_END
DEF_END