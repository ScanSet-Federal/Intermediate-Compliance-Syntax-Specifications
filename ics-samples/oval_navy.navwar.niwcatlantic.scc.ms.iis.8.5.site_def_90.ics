DEF
VAR var_80 string ``
CRI AND
CTN appcmd
    TEST `at least one exists` all
    STATE ste_50
        result string equals `W3C` `all`
    STATE_END
    OBJECT obj_50
        identifier_type `Site`
        identifier `.*`
        parameter `logfile.logFormat`
    OBJECT_END
CTN_END
CTN appcmd
    TEST `at least one exists` all
    STATE ste_90
        result string equals `UserAgent` `all`
    STATE_END
    OBJECT obj_51
        identifier_type `Site`
        identifier `.*`
        parameter `logfile.logExtFileFlags`
    OBJECT_END
CTN_END
CTN appcmd
    TEST `at least one exists` all
    STATE ste_91
        result string equals `(?i)UserName` `all`
    STATE_END
    OBJECT obj_51
        identifier_type `Site`
        identifier `.*`
        parameter `logfile.logExtFileFlags`
    OBJECT_END
CTN_END
CTN appcmd
    TEST `at least one exists` all
    STATE ste_92
        result string equals `(?i)Referer` `all`
    STATE_END
    OBJECT obj_51
        identifier_type `Site`
        identifier `.*`
        parameter `logfile.logExtFileFlags`
    OBJECT_END
CTN_END
CTN appcmd
    TEST `at least one exists` all
    STATE ste_93
        value string equals `sourceName:"Authorization"\s+sourceType:"RequestHeader"` `all`
    STATE_END
    OBJECT obj_80
        var_ref VAR var_80
    OBJECT_END
CTN_END
CTN appcmd
    TEST `at least one exists` all
    STATE ste_94
        value string equals `sourceName:"Content-Type"\s+sourceType:"ResponseHeader"` `all`
    STATE_END
    OBJECT obj_80
        var_ref VAR var_80
    OBJECT_END
CTN_END
CRI_END
DEF_END