DEF
CRI AND
CTN appcmdlistconfig
    TEST `only one exists` all
    STATE ste_270
        result string equals `DetailedLocalOnly` `all`
    STATE_END
    OBJECT obj_270
        identifier_type `Webserver`
        identifier ``
        section `system.webServer/httpErrors`
        parameter `errorMode`
    OBJECT_END
CTN_END
CRI_END
DEF_END