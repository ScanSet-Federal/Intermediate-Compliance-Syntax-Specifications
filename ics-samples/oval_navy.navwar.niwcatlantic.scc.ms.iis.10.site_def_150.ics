DEF
CRI AND
CTN appcmdlistconfig
    TEST `at least one exists` all
    STATE ste_151
        result string equals `SslRequireCert` `all`
    STATE_END
    OBJECT obj_30
        identifier_type `Site`
        identifier `.*`
        section `access`
        parameter `sslflags`
    OBJECT_END
CTN_END
CRI_END
DEF_END