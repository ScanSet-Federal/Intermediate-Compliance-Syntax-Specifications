DEF
CRI OR
CRI AND
CTN appcmdlistconfig
    TEST `only one exists` all
    STATE ste_240
        result string equals `^HMACSHA(256|384|512)$`
    STATE_END
    OBJECT obj_240
        identifier_type `Webserver`
        identifier ``
        section `machineKey`
        parameter `validation`
    OBJECT_END
CTN_END
CTN appcmdlistconfig
    TEST `only one exists` all
    STATE ste_241
        result string equals `Auto` `all`
    STATE_END
    OBJECT obj_241
        identifier_type `Webserver`
        identifier ``
        section `machineKey`
        parameter `decryption`
    OBJECT_END
CTN_END
CRI_END
CTN cmdlet
    TEST `at least one exists` all
    STATE ste_242
        value record equals `
    ` `all`
    STATE_END
    OBJECT obj_242
        module_name `ServerManager`
        module_id ``
        module_version ``
        verb `Get`
        noun `windowsfeature`
        parameters record
            field `Web-Net-Ext45,Web-Net-Ext`
        select record
            field `installstate`
    OBJECT_END
CTN_END
CRI_END
DEF_END