DEF
CRI AND
CTN appcmdlistconfig
    TEST `only one exists` all
    STATE ste_20
        result string equals `Date` `all`
    STATE_END
    OBJECT obj_20
        identifier_type `Webserver`
        identifier ``
        section `system.applicationHost/log`
        parameter `centralW3CLogFile.logExtFileFlags`
    OBJECT_END
CTN_END
CTN appcmdlistconfig
    TEST `only one exists` all
    STATE ste_21
        result string equals `Time` `all`
    STATE_END
    OBJECT obj_20
        identifier_type `Webserver`
        identifier ``
        section `system.applicationHost/log`
        parameter `centralW3CLogFile.logExtFileFlags`
    OBJECT_END
CTN_END
CTN appcmdlistconfig
    TEST `only one exists` all
    STATE ste_22
        result string equals `ClientIP` `all`
    STATE_END
    OBJECT obj_20
        identifier_type `Webserver`
        identifier ``
        section `system.applicationHost/log`
        parameter `centralW3CLogFile.logExtFileFlags`
    OBJECT_END
CTN_END
CTN appcmdlistconfig
    TEST `only one exists` all
    STATE ste_23
        result string equals `UserName` `all`
    STATE_END
    OBJECT obj_20
        identifier_type `Webserver`
        identifier ``
        section `system.applicationHost/log`
        parameter `centralW3CLogFile.logExtFileFlags`
    OBJECT_END
CTN_END
CTN appcmdlistconfig
    TEST `only one exists` all
    STATE ste_24
        result string equals `Method` `all`
    STATE_END
    OBJECT obj_20
        identifier_type `Webserver`
        identifier ``
        section `system.applicationHost/log`
        parameter `centralW3CLogFile.logExtFileFlags`
    OBJECT_END
CTN_END
CTN appcmdlistconfig
    TEST `only one exists` all
    STATE ste_25
        result string equals `UriQuery` `all`
    STATE_END
    OBJECT obj_20
        identifier_type `Webserver`
        identifier ``
        section `system.applicationHost/log`
        parameter `centralW3CLogFile.logExtFileFlags`
    OBJECT_END
CTN_END
CTN appcmdlistconfig
    TEST `only one exists` all
    STATE ste_26
        result string equals `HttpStatus` `all`
    STATE_END
    OBJECT obj_20
        identifier_type `Webserver`
        identifier ``
        section `system.applicationHost/log`
        parameter `centralW3CLogFile.logExtFileFlags`
    OBJECT_END
CTN_END
CTN appcmdlistconfig
    TEST `only one exists` all
    STATE ste_27
        result string equals `Referer` `all`
    STATE_END
    OBJECT obj_20
        identifier_type `Webserver`
        identifier ``
        section `system.applicationHost/log`
        parameter `centralW3CLogFile.logExtFileFlags`
    OBJECT_END
CTN_END
CTN appcmdlistconfig
    TEST `only one exists` all
    STATE ste_52
        result string equals `W3C` `all`
    STATE_END
    OBJECT obj_51
        identifier_type `Webserver`
        identifier ``
        section `system.applicationHost/sites`
        parameter `siteDefaults.logFile.logFormat`
    OBJECT_END
CTN_END
CRI_END
DEF_END