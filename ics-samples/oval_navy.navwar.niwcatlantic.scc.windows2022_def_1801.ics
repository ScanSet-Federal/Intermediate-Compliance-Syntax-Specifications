DEF
CRI AND
CTN cmdlet
    TEST `at least one exists` `only one`
    STATE ste_1801
        value record equals `
    ` `only one`
    STATE_END
    OBJECT obj_1801
        module_name `CimCmdlets`
        module_id ``
        module_version ``
        verb `Get`
        noun `CimInstance`
        parameters record
            field `root\Microsoft\Windows\DeviceGuard`
        select record
            field `RequiredSecurityProperties`
    OBJECT_END
CTN_END
CTN cmdlet
    TEST `at least one exists` `only one`
    STATE ste_1802
        value record equals `
    ` `only one`
    STATE_END
    OBJECT obj_1802
        module_name `CimCmdlets`
        module_id ``
        module_version ``
        verb `Get`
        noun `CimInstance`
        parameters record
            field `root\Microsoft\Windows\DeviceGuard`
        select record
            field `VirtualizationBasedSecurityStatus`
    OBJECT_END
CTN_END
CRI_END
DEF_END