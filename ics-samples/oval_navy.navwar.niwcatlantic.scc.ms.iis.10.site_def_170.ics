DEF
CRI AND
CTN appcmdlistconfig
    TEST `at least one exists` all
    STATE ste_170
        result string equals `InProc` `all`
    STATE_END
    OBJECT obj_170
        identifier_type `Site`
        identifier `.*`
        section `system.web/sessionState`
        parameter `mode`
    OBJECT_END
CTN_END
CRI_END
DEF_END