DEF
CRI AND
CTN wmi
    TEST `at least one exists` all
    STATE ste_470000
        result int `less than or equal` `5`
    STATE_END
    OBJECT obj_470000
        namespace `root\rsop\computer`
        wql `SELECT setting FROM RSOP_SecuritySettingNumeric WHERE KeyName='MaxClockSkew'`
    OBJECT_END
CTN_END
CRI_END
DEF_END