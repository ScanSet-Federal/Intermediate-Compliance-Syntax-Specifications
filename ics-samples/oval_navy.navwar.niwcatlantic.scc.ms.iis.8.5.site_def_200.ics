DEF
CRI AND
CTN appcmdlistconfig
    TEST `at least one exists` all
    STATE ste_200
        result int `less than or equal` `30000000` `all`
    STATE_END
    OBJECT obj_200
        identifier_type `Site`
        identifier `.*`
        section `requestfiltering`
        parameter `requestLimits.maxAllowedContentLength`
    OBJECT_END
CTN_END
CRI_END
DEF_END