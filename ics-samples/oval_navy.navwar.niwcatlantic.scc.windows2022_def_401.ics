DEF
CRI AND
CTN cmdlet
    TEST `at least one exists` `at least one`
    STATE ste_401
        value record equals `
    `
    STATE_END
    OBJECT obj_401
        module_name `TrustedPlatformModule`
        module_id ``
        module_version ``
        verb `Get`
        noun `Tpm`
        select record
            field `tpmpresent,tpmready,tpmenabled`
    OBJECT_END
CTN_END
CTN cmdlet
    TEST `at least one exists` `at least one`
    STATE ste_402
        value record equals `
    `
    STATE_END
    OBJECT obj_402
        module_name `CimCmdlets`
        module_id ``
        module_version ``
        verb `Get`
        noun `CimInstance`
        parameters record
            field `root\cimv2\security\microsofttpm`
        select record
            field `specversion`
    OBJECT_END
CTN_END
CRI_END
DEF_END