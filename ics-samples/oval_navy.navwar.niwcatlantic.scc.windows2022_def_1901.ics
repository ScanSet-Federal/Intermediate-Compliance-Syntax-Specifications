DEF
CRI AND
CTN cmdlet
    TEST `none exist` all
    OBJECT obj_1901
        module_name `ActiveDirectory`
        module_id ``
        module_version ``
        verb `Get`
        noun `ADUser`
        parameters record
            field `(Enabled -eq $True) -and (SmartcardLogonRequired -eq $False)`
        select record
            field `Name`
    OBJECT_END
CTN_END
CRI_END
DEF_END