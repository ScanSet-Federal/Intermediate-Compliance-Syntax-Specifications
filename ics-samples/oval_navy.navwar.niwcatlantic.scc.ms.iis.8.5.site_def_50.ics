DEF
CRI AND
CTN appcmd
    TEST `at least one exists` all
    STATE ste_50
        result string equals `W3C` `all`
    STATE_END
    OBJECT obj_50
        identifier_type `Site`
        identifier `.*`
        parameter `logfile.logFormat`
    OBJECT_END
CTN_END
CTN appcmd
    TEST `at least one exists` all
    STATE ste_51
        result string equals `(?i)Date` `all`
    STATE_END
    OBJECT obj_51
        identifier_type `Site`
        identifier `.*`
        parameter `logfile.logExtFileFlags`
    OBJECT_END
CTN_END
CTN appcmd
    TEST `at least one exists` all
    STATE ste_52
        result string equals `(?i)Time` `all`
    STATE_END
    OBJECT obj_51
        identifier_type `Site`
        identifier `.*`
        parameter `logfile.logExtFileFlags`
    OBJECT_END
CTN_END
CTN appcmd
    TEST `at least one exists` all
    STATE ste_53
        result string equals `(?i)ClientIP` `all`
    STATE_END
    OBJECT obj_51
        identifier_type `Site`
        identifier `.*`
        parameter `logfile.logExtFileFlags`
    OBJECT_END
CTN_END
CTN appcmd
    TEST `at least one exists` all
    STATE ste_54
        result string equals `(?i)UserName` `all`
    STATE_END
    OBJECT obj_51
        identifier_type `Site`
        identifier `.*`
        parameter `logfile.logExtFileFlags`
    OBJECT_END
CTN_END
CTN appcmd
    TEST `at least one exists` all
    STATE ste_55
        result string equals `(?i)Method` `all`
    STATE_END
    OBJECT obj_51
        identifier_type `Site`
        identifier `.*`
        parameter `logfile.logExtFileFlags`
    OBJECT_END
CTN_END
CTN appcmd
    TEST `at least one exists` all
    STATE ste_56
        result string equals `(?i)UriQuery` `all`
    STATE_END
    OBJECT obj_51
        identifier_type `Site`
        identifier `.*`
        parameter `logfile.logExtFileFlags`
    OBJECT_END
CTN_END
CTN appcmd
    TEST `at least one exists` all
    STATE ste_57
        result string equals `(?i)HttpStatus` `all`
    STATE_END
    OBJECT obj_51
        identifier_type `Site`
        identifier `.*`
        parameter `logfile.logExtFileFlags`
    OBJECT_END
CTN_END
CTN appcmd
    TEST `at least one exists` all
    STATE ste_58
        result string equals `(?i)Referer` `all`
    STATE_END
    OBJECT obj_51
        identifier_type `Site`
        identifier `.*`
        parameter `logfile.logExtFileFlags`
    OBJECT_END
CTN_END
CRI_END
DEF_END