DEF
CRI AND
CTN rpminfo
    TEST `at least one exists` all
    OBJECT obj_1801
        name `opensc`
    OBJECT_END
CTN_END
CTN shellcommand
    TEST `at least one exists` all
    OBJECT obj_1802
        shell `bash`
        command `opensc-tool --list-drivers | grep -i piv`
        pattern `^\s*PIV-II\s`
    OBJECT_END
CTN_END
CRI_END
DEF_END