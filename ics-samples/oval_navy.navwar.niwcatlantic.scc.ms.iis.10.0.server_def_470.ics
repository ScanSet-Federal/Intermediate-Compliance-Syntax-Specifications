DEF
CRI AND
CTN appcmdlistconfig
    TEST `only one exists` all
    STATE ste_470
        result string equals `X-Powered-By` `all`
    STATE_END
    OBJECT obj_470
        identifier_type `Webserver`
        identifier ``
        section `system.webServer/httpProtocol`
        parameter `all`
    OBJECT_END
CTN_END
CRI_END
DEF_END