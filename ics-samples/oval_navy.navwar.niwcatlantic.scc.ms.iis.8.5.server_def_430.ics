DEF
CRI AND
CTN appcmdlistconfig
    TEST `only one exists` all
    STATE ste_430
        result int `not equal` `0` `all`
    STATE_END
    OBJECT obj_430
        identifier_type `Webserver`
        identifier ``
        section `system.applicationHost/sites`
        parameter `siteDefaults.limits.maxConnections`
    OBJECT_END
CTN_END
CRI_END
DEF_END